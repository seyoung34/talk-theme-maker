# CLAUDE.md

This project's knowledge base is maintained in `AGENTS.md` files (shared with codex).
Claude Code reads it via the import below — keep `AGENTS.md` as the single source of truth.

@AGENTS.md
