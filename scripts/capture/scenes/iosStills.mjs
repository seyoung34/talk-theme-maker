// iOS 뒷부분 스텝의 장면 구성. 실기기 스크린샷을 이어 붙여 클립으로 만든다(`stills.mjs`).
//
// **좌표는 각 스크린샷 안의 좌표다(1206x2432).** 여러 장을 나란히 붙여 놓고 좌표를 읽으면
// 이어 붙인 시트의 절대 좌표가 나와 x가 1206을 넘는다 — 실제로 그렇게 넣어 박스가 화면 밖으로
// 나갔다. 장마다 따로 재야 한다.
//
// **좌표는 원본 스크린샷(1206x2432) 기준이다.** 상단 190px(상태바)을 잘라낸 뒤의 크기이므로,
// 다시 찍어 자르는 규격이 달라지면 여기도 함께 바꿔야 한다.
//
// 스크린샷 순서는 실기기에서 시간순으로 찍은 그대로다. 중간을 건너뛰지 않는 것이 요점이라,
// "이 화면 다음에 저 화면이 나온다"가 전달되어야 사용자가 자기 화면과 대조할 수 있다.
import path from "node:path";

/** 잘라 둔 스크린샷이 있는 곳. 원본은 `가이드 자료/`에 그대로 두고 이쪽만 쓴다. */
export const iosStillsDir = "E:\\TalkTheme-자료\\가이드 자료-crop";
const base = "KakaoTalk_20260815_033419862";

const shot = (suffix) => path.join(iosStillsDir, suffix ? `${base}_${suffix}.png` : `${base}.png`);

/**
 * 스텝 8 — 파일 받기.
 *
 * **iOS에서 가장 막히는 곳이다.** Safari가 받은 파일은 화면에 남지 않고, `···` 메뉴 안
 * '다운로드'에 들어 있다. 그 경로를 모르면 "받았다는데 어디 있지"에서 멈춘다. Android의
 * '알 수 없는 앱 설치 허용'과 같은 자리다.
 *
 * 그래서 네 장을 다 쓴다. 확인 → 받음 표시 → 메뉴 → 목록까지 이어 보여야 경로가 전달된다.
 */
export const iosDownloadFile = {
  id: "ios-download-file",
  title: "파일 받기",
  shots: [
    // Safari가 다운로드를 한 번 더 묻는다. 이 확인을 놓치면 아무 일도 일어나지 않는다.
    { file: shot("02"), highlight: { x: 885, y: 1115, w: 230, h: 100 }, seconds: 2.4 },
    // 주소창에 받음 표시가 생긴다. 화면은 그대로라 이 아이콘이 유일한 신호다.
    { file: shot("03"), highlight: { x: 295, y: 2205, w: 105, h: 105 }, seconds: 2.2 },
    // `···` 메뉴 안의 '다운로드'. 이 스텝의 핵심이다.
    { file: shot("04"), highlight: { x: 100, y: 1835, w: 1010, h: 150 }, seconds: 2.6 },
    // 받은 파일이 목록에 있다. 눌러서 다음 스텝으로 넘어간다.
    { file: shot("05"), highlight: { x: 60, y: 1280, w: 1100, h: 175 }, seconds: 2.4 },
  ],
};

/**
 * 스텝 7 — 테마 파일 만들기.
 *
 * **여기까지가 우리 화면이다.** 다음 스텝부터는 Safari와 카카오톡에서 일어난다.
 *
 * 촬영으로 대신할 수 없다. 다운로드 창을 여는 것까지는 찍을 수 있지만 "생성 중" 화면은 실제로
 * 제출해야 나오고, 그러면 크레딧이 나가고 빌드가 돈다. 로컬에서는 GCS 인증이 없어 생성 중이
 * 아니라 실패 화면이 뜬다. 앞뒤가 스크린샷이므로 이 스텝도 스크린샷으로 통일한다.
 */
export const iosMakeFile = {
  id: "ios-make-file",
  title: "테마 파일 만들기",
  shots: [
    // 다운로드 창. 형식이 .ktheme 하나뿐이라 Android처럼 고를 것이 없고, 확인할 것은 이름과 크레딧이다.
    { file: shot(), highlight: { x: 745, y: 1600, w: 335, h: 115 }, seconds: 2.8 },
    // 생성에 몇 초 걸린다. **창을 닫으면 안 된다**는 것이 이 장면의 내용이라 조금 길게 둔다.
    { file: shot("01"), seconds: 2.6 },
  ],
};

/**
 * 스텝 9 — 카카오톡으로 보내 적용하기.
 *
 * iOS는 받은 파일을 **카카오톡을 거쳐** 열어야 테마가 적용된다. Android가 APK를 바로 설치하는
 * 것과 가장 크게 갈리는 지점이고, 스텝을 따로 둔 이유이기도 하다.
 *
 * 자기 자신에게 보내는 것이 요점이라 친구 목록은 가려 두었다(개인정보이기도 하다). 남긴 '나에게'가
 * 이 화면에서 눌러야 할 곳이다.
 */
export const iosApplyTheme = {
  id: "ios-apply-theme",
  title: "카카오톡으로 보내 적용하기",
  shots: [
    // 받은 파일을 열면 미리보기가 뜬다. 아래 카카오톡 버튼이 공유 입구다.
    { file: shot("06"), highlight: { x: 470, y: 2215, w: 265, h: 120 }, seconds: 2.4 },
    // 공유 시트. 카카오톡을 고른다.
    { file: shot("07"), highlight: { x: 55, y: 1655, w: 215, h: 225 }, seconds: 2.4 },
    // 보낼 곳을 고른다. 친구에게 보낼 필요 없이 '나에게'면 된다.
    { file: shot("08"), highlight: { x: 55, y: 2080, w: 160, h: 155 }, seconds: 2.6 },
    // 대화방에 도착한 파일. '열기'를 누른다.
    { file: shot("09"), highlight: { x: 995, y: 2040, w: 160, h: 110 }, seconds: 2.4 },
    // 마지막. 여기서 '테마 적용하기'를 누르면 끝난다.
    { file: shot("10"), highlight: { x: 165, y: 910, w: 875, h: 135 }, seconds: 2.8 },
  ],
};
