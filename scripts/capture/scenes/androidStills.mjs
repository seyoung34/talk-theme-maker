// Android 설치·적용 스텝의 장면 구성. 실기기 스크린샷을 이어 붙여 클립으로 만든다(`stills.mjs`).
//
// **이 스텝만 촬영이 불가능하다.** 앞 일곱 스텝은 전부 우리 페이지 안에서 일어나지만 여기서는
// 파일 앱, 패키지 설치 프로그램, Play 프로텍트, 그리고 설치된 테마 앱까지 네 개의 다른 앱을
// 오간다. 브라우저를 보는 촬영 도구로는 한 화면도 담을 수 없다. iOS 뒷부분과 같은 이유다.
//
// **좌표는 각 스크린샷 안의 좌표다(1080x2123).** 여러 장을 붙여 놓고 재면 시트의 절대 좌표가
// 나와 박스가 화면 밖으로 나간다 — iOS 때 실제로 그렇게 넣었다. 장마다 따로 재야 한다.
//
// 원본은 크기가 한 장만 달랐다(1080x2190). 상단이 67px 덜 잘린 것이라 위쪽을 그만큼 깎아
// 나머지와 맞췄다. **규격이 섞이면 concat이 조용히 멈춘다** — 오류도 경고도 없이 클립이 첫
// 장 길이로 끝난다. `가이드 자료-crop-android`에 맞춰 둔 것만 쓰는 이유다.
import path from "node:path";

/** 규격을 맞춰 둔 스크린샷. 원본은 `가이드 자료-android/`에 그대로 둔다. */
export const androidStillsDir = "E:\\TalkTheme-자료\\가이드 자료\\가이드 자료-crop-android";

const shot = (index) => path.join(androidStillsDir, `android-${String(index).padStart(2, "0")}.png`);

/**
 * 스텝 8 — 설치하고 카톡에 적용하기.
 *
 * **가이드에서 가장 막히는 스텝이다.** 그런데 막히는 지점이 예상과 달랐다. 설정에 들어가
 * '알 수 없는 앱 설치'를 켜는 화면은 나오지 않고, 대신 **Play 프로텍트가 설치를 차단한다.**
 *
 * 차단 창(④)에는 큰 파란 버튼이 '확인' 하나뿐이다. 설치하는 길은 `세부정보 더보기`를 펼쳐야
 * 나오는 작은 회색 글씨 '무시하고 설치하기'(⑤)다. 접힌 상태만 보면 차단됐다고 읽고 확인을
 * 눌러 끝난다 — iOS Safari의 `···` 메뉴 속 '다운로드'와 정확히 같은 함정이다.
 *
 * 그래서 ④와 ⑤를 둘 다 쓴다. 한 장으로 줄이면 "펼쳐야 나온다"가 사라진다.
 *
 * 일곱 장은 다른 클립보다 길지만, 이 스텝은 눌러야 할 곳이 일곱 번 나온다. 줄이려면 화면을
 * 빼야 하고, 빼는 순간 사용자가 자기 화면과 대조할 수 없게 된다.
 */
export const androidInstallApply = {
  id: "android-install-apply",
  title: "설치하고 카톡에 적용하기",
  shots: [
    // 파일 앱에서 APK를 열면 어느 앱으로 열지 묻는다. '한 번만'이면 충분하다.
    { file: shot(1), highlight: { x: 228, y: 2030, w: 146, h: 52 }, seconds: 2.4 },
    // 설치 확인. 앱 이름이 내가 만든 테마 이름인지 여기서 확인할 수 있다.
    { file: shot(2), highlight: { x: 710, y: 1145, w: 105, h: 66 }, seconds: 2.0 },
    // 스토어를 거치지 않은 앱이라 경고가 뜬다. 내가 만든 파일이므로 그대로 진행한다.
    { file: shot(3), highlight: { x: 195, y: 1945, w: 275, h: 66 }, seconds: 2.6 },
    // Play 프로텍트 차단. 이 상태에서는 '확인'밖에 없다. 아래 화살표를 펼쳐야 한다.
    { file: shot(4), highlight: { x: 138, y: 1248, w: 310, h: 56 }, seconds: 2.2 },
    // 펼치면 '무시하고 설치하기'가 나온다. 이 스텝의 핵심이다.
    { file: shot(5), highlight: { x: 142, y: 1328, w: 272, h: 56 }, seconds: 2.8 },
    // 설치 완료. '열기'로 바로 테마 앱을 띄운다.
    { file: shot(6), highlight: { x: 718, y: 1145, w: 95, h: 66 }, seconds: 2.0 },
    // 테마 앱. 아래 '테마 적용하기'를 누르면 카카오톡 테마 설정이 열린다.
    { file: shot(7), highlight: { x: 48, y: 1938, w: 985, h: 135 }, seconds: 2.6 },
  ],
};
